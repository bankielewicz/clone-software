# {{PRODUCT_NAME}}

Dependency-free Python src-layout scaffold for `{{PRODUCT_TYPE}}`. It uses the Python standard library only.

```text
python3 -m unittest discover -s tests -v
python3 -m compileall -q src
python3 src/clone_app/__main__.py
```

Replace the neutral command contract only after the clone specification and architecture decision pin the public interface.
