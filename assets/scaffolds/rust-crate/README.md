# {{PRODUCT_NAME}}

Dependency-free Rust crate scaffold for `{{PRODUCT_TYPE}}`. It uses the Rust standard library and Cargo's built-in test runner only.

```text
cargo test
cargo build
cargo run
```

Replace the fixed internal crate name only as an explicit architecture decision; public product naming remains `{{PRODUCT_NAME}}`.
