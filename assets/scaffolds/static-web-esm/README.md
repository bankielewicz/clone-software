# {{PRODUCT_NAME}}

Neutral static-web scaffold for the authorized `{{PRODUCT_TYPE}}` reimplementation described by {{SOURCE_DESCRIPTION}}.

Requirements: a current Node.js/npm toolchain for the smoke test and Python 3 for the local static server.

```text
npm test
npm start
```

The development server binds to `127.0.0.1:8000`. Pin any different host or port and its authorization boundary in the build plan before changing that command.
