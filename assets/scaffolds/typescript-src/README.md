# {{PRODUCT_NAME}}

Dependency-free strict TypeScript ESM scaffold for `{{PRODUCT_TYPE}}`. It records no npm dependencies and requires Node.js 22.18 or newer, which executes erasable TypeScript syntax with built-in type stripping.

```text
npm test
npm run build
npm start
```

`tsconfig.json` records the intended static-analysis contract for an authorized external compiler, but the default commands install nothing. Pin exact toolchain versions in the clone pack before implementation or release.
